/* State-first explorer: intentionally removes regional navigation. */
(function(){
'use strict';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function render(){const root=document.getElementById('stateExplorer');if(!root)return;const d=window.INDIA_DIRECTORY||{};const cities=typeof window.cities!=='undefined'?window.cities:{};const states=Object.keys(d).sort((a,b)=>a.localeCompare(b));root.innerHTML=states.map((state,i)=>{const list=[...new Set(d[state]||[])].sort();const verified=list.filter(c=>!!cities[c]).length;return `<details class="region-panel state-first" ${state==='Andhra Pradesh'?'open':''}><summary class="region-summary"><span class="region-symbol">✦</span><span class="region-copy"><small>${state.startsWith('Andhra')?'PRIORITY RESEARCH STATE':'STATE / UNION TERRITORY'}</small><strong>${esc(state)}</strong><em>${list.length} destinations · ${verified} existing curated city profiles</em></span><span class="region-count">${list.length} cities <b>↓</b></span></summary><div class="region-content"><div class="region-rule"><span>CITIES & DESTINATIONS</span><i>Open a city to explore history, culture, places, photos and transport</i></div><div class="state-cities">${list.map(city=>{const ok=!!cities[city];return `<a class="city-explorer-card ${ok?'is-verified':'is-directory'}" href="city.html?city=${encodeURIComponent(city)}"><span class="city-card-region">${esc(state)}</span><strong>${esc(city)}</strong><span class="city-card-detail">${ok?'Curated places · history · culture':'City profile · research and verified places being added'}</span><span class="city-card-arrow">Explore city ↗</span></a>`}).join('')}</div></div></details>`}).join('');root.classList.add('region-explorer-ready')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',render,{once:true});else render();
})();
