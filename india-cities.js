(function(){'use strict';const root=document.getElementById('indiaDirectory');const input=document.getElementById('cityFilter');const directory=window.INDIA_DIRECTORY||{};function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function render(){const q=(input.value||'').trim().toLowerCase();let html='';Object.keys(directory).sort().forEach(state=>{const cities=(directory[state]||[]).filter(c=>!q||state.toLowerCase().includes(q)||c.toLowerCase().includes(q));if(!cities.length)return;html+=`<section class="place-card city-browser"><h2>${esc(state)} <span style="font-size:13px;color:#718078;font-weight:600">${cities.length} destinations</span></h2><div class="city-list">${cities.sort().map(city=>`<a href="discover.html?city=${encodeURIComponent(city)}"><strong>${esc(city)}</strong><br><span style="font-size:12px;color:#718078">Explore places →</span></a>`).join('')}</div></section>`});root.innerHTML=html||'<div class="place-card"><strong>No matching city found.</strong><p style="color:#68736d">Try another city or state.</p></div>'}input.addEventListener('input',render);render();})();