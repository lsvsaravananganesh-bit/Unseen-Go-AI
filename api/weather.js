export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'GET required'});const {latitude,longitude,city}=req.query||{};if(!latitude||!longitude)return res.status(400).json({error:'latitude and longitude are required'});try{const u=new URL(process.env.WEATHER_BASE_URL||'https://api.open-meteo.com/v1/forecast');u.searchParams.set('latitude',latitude);u.searchParams.set('longitude',longitude);u.searchParams.set('current','temperature_2m,relative_humidity_2m,apparent_temperature,is_day,precipitation,weather_code,wind_speed_10m');u.searchParams.set('daily','weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max,sunrise,sunset');u.searchParams.set('forecast_days','4');u.searchParams.set('timezone','auto');const r=await fetch(u);const data=await r.json();if(!r.ok)return res.status(r.status).json({error:data.reason||'Weather provider error'});return res.status(200).json({city:city||null,source:'Open-Meteo',data})}catch(e){return res.status(502).json({error:e.message||'Weather provider error'})}}