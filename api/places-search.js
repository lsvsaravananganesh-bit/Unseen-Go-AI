export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'GET required'});const key=process.env.GOOGLE_MAPS_API_KEY;if(!key)return res.status(503).json({error:'Google Places is not configured. Add GOOGLE_MAPS_API_KEY in Vercel.'});const q=String(req.query.q||'').trim(),city=String(req.query.city||'').trim();if(!q&&!city)return res.status(400).json({error:'q or city is required'});const textQuery=[q,city].filter(Boolean).join(' in ');try{const r=await fetch('https://places.googleapis.com/v1/places:searchText',{method:'POST',headers:{'Content-Type':'application/json','X-Goog-Api-Key':key,'X-Goog-FieldMask':'places.id,places.displayName,places.formattedAddress,places.location,places.rating,places.userRatingCount,places.googleMapsUri,places.photos,places.primaryType,places.types,places.businessStatus,places.regularOpeningHours,places.editorialSummary'},body:JSON.stringify({textQuery,languageCode:'en',maxResultCount:20})});const data=await r.json();if(!r.ok)return res.status(r.status).json({error:data.error?.message||'Places request failed'});res.setHeader('Cache-Control','s-maxage=300,stale-while-revalidate=600');return res.status(200).json({source:'Google Places API (New)',places:data.places||[]})}catch(e){return res.status(502).json({error:e.message||'Places provider error'})}}