const CONFIG=require('./unseengo-config');
module.exports=async function handler(req,res){try{if(!CONFIG.placesApiKey)return res.status(503).json({error:'Google Places is not configured. Add GOOGLE_MAPS_API_KEY in the server environment.'});const {textQuery,lat,lng}=req.query||{};if(!textQuery)return res.status(400).json({error:'textQuery is required'});const body={textQuery};if(lat&&lng)body.locationBias={circle:{center:{latitude:Number(lat),longitude:Number(lng)},radius:10000}};const r=await fetch('https://places.googleapis.com/v1/places:searchText',{method:'POST',headers:{'Content-Type':'application/json','X-Goog-Api-Key':CONFIG.placesApiKey,'X-Goog-FieldMask':'places.id,places.displayName,places.formattedAddress,places.location,places.rating,places.userRatingCount,places.priceLevel,places.currentOpeningHours,places.websiteUri,places.nationalPhoneNumber,places.googleMapsUri,places.photos'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw new Error(data.error?.message||'Places provider error');res.status(200).json({source:'Google Places API (New)',places:data.places||[]});}catch(e){res.status(502).json({error:e.message});}};
