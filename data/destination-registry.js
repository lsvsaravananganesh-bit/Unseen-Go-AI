/*
 * UnseenGo AI — Canonical Destination Registry
 * Compatibility-first data layer. Existing `cities` remains the source of truth.
 */
(function () {
  'use strict';
  const CATEGORY_ALIASES = {Nature:'nature',Heritage:'heritage',Food:'food',Culture:'culture',Adventure:'adventure',Wildlife:'wildlife',Photography:'photography',Temples:'temples',Forts:'forts',Shopping:'shopping',Festivals:'festivals',Stays:'stays',Transport:'transport',Experiences:'experiences'};
  const SKIP_KEYS = new Set(['region']);
  const clean = value => String(value ?? '').replace(/\s+/g,' ').trim();
  const slugify = value => clean(value).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');
  function canonicalCategory(category){const key=clean(category);return CATEGORY_ALIASES[key]||key.toLowerCase().replace(/\s+/g,'-');}
  function parseLocation(rawLocation,cityName){const raw=clean(rawLocation);const parts=raw.split('·').map(clean).filter(Boolean);const locality=parts[0]||cityName;const distanceText=parts.slice(1).join(' · ');const isCity=!distanceText||/^city$/i.test(distanceText);const m=distanceText.match(/(\d+(?:\.\d+)?)\s*km/i);return{label:raw,locality,distanceText:distanceText||null,distanceKm:m?Number(m[1]):null,withinCity:isCity};}
  function normalizePlace(cityName,region,category,row,index){if(!Array.isArray(row))return null;const name=clean(row[0]);if(!name)return null;const location=parseLocation(row[1],cityName);const legacyScore=Number(row[2]);const normalizedCategory=canonicalCategory(category);return{id:`${slugify(cityName)}-${slugify(name)||index+1}`,identity:{name,city:cityName,state:null,region:clean(region)||null,locality:location.locality,coordinates:null},classification:{categories:[normalizedCategory],destinationType:normalizedCategory},experience:{nature:null,heritage:null,culture:null,food:null,adventure:null,wildlife:null,photography:null,localCommunity:null},travel:{distanceKm:location.distanceKm,distanceText:location.distanceText,withinCity:location.withinCity,travelModes:[],accessibility:null,estimatedTime:null},recommendationSignals:{hiddenness:Number.isFinite(legacyScore)?legacyScore:null,culturalValue:null,travelEase:null,localExperience:null,budgetLevel:null,paceLevel:null,crowdLevel:null},sustainability:{crowdLoad:null,environmentalImpact:null,localEconomicOpportunity:null,carbonFactor:null,score:null,dataSource:null,verificationStatus:'not-measured'},content:{description:clean(row[3]),history:null,image:null,source:null,sourceUrl:null},legacy:{category,locationLabel:location.label,score:Number.isFinite(legacyScore)?legacyScore:null}};}
  function buildRegistry(source){const registry=[];const seen=new Set();Object.entries(source||{}).forEach(([cityName,cityData])=>{if(!cityData||typeof cityData!=='object')return;Object.entries(cityData).forEach(([category,rows])=>{if(SKIP_KEYS.has(category)||!Array.isArray(rows))return;rows.forEach((row,index)=>{const place=normalizePlace(cityName,cityData.region,category,row,index);if(!place||seen.has(place.id))return;seen.add(place.id);registry.push(place);});});});return registry;}
  function summarize(registry){const cities=new Set(),categories=new Set();registry.forEach(p=>{cities.add(p.identity.city);p.classification.categories.forEach(c=>categories.add(c));});return{destinations:registry.length,cities:cities.size,categories:[...categories].sort(),measuredSustainability:registry.filter(p=>p.sustainability.verificationStatus!=='not-measured').length,sourceModel:'legacy-cities-compatible-normalized'};}
  let source={};try{source=typeof cities!=='undefined'&&cities?cities:(window.cities||{});}catch(_){source=window.cities||{};}
  const destinations=buildRegistry(source);window.UnseenGoData=window.UnseenGoData||{};window.UnseenGoData.destinations=destinations;window.UnseenGoData.categories=[...new Set(destinations.flatMap(p=>p.classification.categories))].sort();window.UnseenGoData.summary=summarize(destinations);window.UnseenGoData.schemaVersion='1.1';window.UnseenGoData.normalize=buildRegistry;
})();
